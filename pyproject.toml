[project]
name = "django-new-contributor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "feedgen>=1.0.0",
    "feedparser>=6.0.12",
]

[dependency-groups]
dev = [
    "ruff>=0.14.13",
]
